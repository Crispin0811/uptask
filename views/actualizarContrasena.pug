<!DOCTYPE html>
html(lang="en")
    head
        include partial/header

    body.login
        .contenedor-formulario
            h1 #{nombrePagina}
            p Coloca tu nuevo Contraseña


            form#formulario.caja-login(method="post")
                .campo
                    label(for="password") Contraseña
                    input(type="password" name="password" placeholder="Nueva Contraseña" )
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer Constraseña" )