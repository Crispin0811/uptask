<!DOCTYPE html>
html(lang="en")
    head
        include partial/header

    body.login
        .contenedor-formulario
            h1 #{nombrePagina}
            p Agrega tu Email para reestablecer tu contraseña

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja-login(action="/reestablecer" method="post")
                .campo
                    label(for="email") E-mail
                    input(type="email" name="email" placeholder="Tu email" )
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer Constraseña" )
                .campo.acciones
                    a(href="/iniciar-sesion") Iniciar Sesión
